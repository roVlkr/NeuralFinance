<UserControl x:Name="marker"
             x:Class="NeuralFinance.View.DiagramControls.Axis.YMarker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeuralFinance.View.DiagramControls.Axis"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Canvas>
        <Grid x:Name="mainGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="6"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding ElementName=marker, Path=Description}" Margin="0 0 5 0"/>

            <Grid Grid.Column="1" VerticalAlignment="Center">
                <Line
                  X2="{Binding RelativeSource={RelativeSource AncestorType=Grid, Mode=FindAncestor}, Path=ActualWidth}"
                  Stroke="Black"/>
            </Grid>
        </Grid>
    </Canvas>    
</UserControl>
